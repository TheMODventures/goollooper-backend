export enum EUserRole {
  admin = 1,
  user = 2,
  serviceProvider = 3,
}

export enum EUserLocationType {
  global = "global",
  local = "local",
}

export enum ServiceType {
  volunteer = "volunteer",
  interest = "interest",
}
